import { environment } from '../../environments/environment';

export namespace Constants {
  export const BASE_URL = environment.constants.BASE_URL;
  export const API_URL = environment.constants.API_URL;
  export const GEOSERVER_URL = environment.constants.GEOSERVER_URL;
}
